<%= form_with(model: ticket_list) do |form| %>
  <% if ticket_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ticket_list.errors.count, "error") %> prohibited this ticket_list from being saved:</h2>

      <ul>
        <% ticket_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.label :ticket_id, style: "display: block" %>
    <%= form.text_field :ticket_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
