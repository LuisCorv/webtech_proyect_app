<div class="card mx-auto w-50 p-3 bg-white text-black text-center">



<h1>Editing assign ticket</h1>

<%= render "form", assign_ticket: @assign_ticket %>

<br>

<div>
  <%= link_to "Show this assign ticket", user_assign_ticket_path(@assign_ticket.user,@assign_ticket), class:"btn btn-sm btn-warning mt-2" %> 
  <%= link_to "Back to assign tickets", user_assign_tickets_path(@assign_ticket.user), class:"btn btn-sm btn-secondary mt-2" %>
</div>

</div>