<%= form_with(model: tag_list) do |form| %>
  <% if tag_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tag_list.errors.count, "error") %> prohibited this tag_list from being saved:</h2>

      <ul>
        <% tag_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :ticket_id, style: "display: block" %>
    <%= form.text_field :ticket_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
