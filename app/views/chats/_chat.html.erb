<div id="<%= dom_id chat %>">
  <p>
    <strong>Ticket title:</strong>
    <%= chat.ticket.title %>
  </p>

  <p>
    <strong>Comments:</strong>
    <% chat.comments.each do |t| %>
    <ul>
      <li><%= t.display_comment %></li>
    </ul>
    <%end %>
  </p>
</div>
